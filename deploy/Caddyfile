# Dedicated host example. Replace this reserved domain before starting Caddy.
prompt.example.com {
    encode zstd gzip
    header Strict-Transport-Security "max-age=31536000"
    reverse_proxy 127.0.0.1:8080
}
